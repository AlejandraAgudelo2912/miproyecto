<x-blog-layout>
    <x-slot name="header" class="text-white">Asteroides Cercanos a la Tierra</x-slot>

    <ul class="mt-4 space-y-4">
        @foreach ($data['near_earth_objects'] as $date => $asteroids)
            <li class="border-b pb-4">
                <h3 class="font-bold text-xl">Fecha: {{ $date }}</h3>
                <ul class="mt-2">
                    @foreach ($asteroids as $asteroid)
                        <li class="bg-gray-100 p-4 rounded shadow">
                            <strong>{{ $asteroid['name'] }}</strong> - Tamaño: {{ $asteroid['estimated_diameter']['meters']['estimated_diameter_max'] }}m
                        </li>
                    @endforeach
                </ul>
            </li>
        @endforeach
    </ul>
</x-blog-layout>

